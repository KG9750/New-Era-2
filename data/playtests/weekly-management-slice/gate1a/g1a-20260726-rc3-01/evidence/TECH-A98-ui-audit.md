# Gate 1A RC3 独立技术 UI 审计

## 身份与边界

- 审计编号：`TECH-A98`
- UI 匿名编号：`A98`
- 计入 Gate 1A：否
- 执行方式：全新独立 agent，只使用玩家页面
- 禁止项：未读取或修改仓库、源码、测试、Issue、开发者控制台或配置
- 结论：`PASS`，无 UI 流程阻断

本记录只支持 RC 冻结复核，不是 A01–A07 正式样本，也不产生真人体验结论。

## 可见 RC 元数据

| 字段 | UI 可见值 |
|---|---|
| build ID | `g1-rc-20260726.3` |
| Git SHA | `57621e670e69a2b4c7c283a6614ab1bdefd71ca1` |
| artifact hash | `6c173733662fdb79160d6188c4fb8883d87d2e68dea5c47c632e373b13b2c725` |
| initial state hash | `fnv1a32-33a16fbf` |
| 场景版本 | `0.4.0` |
| 固定种子 | `104729` |
| Session | `d000352f-a18c-4f39-ac07-32e6c1388d45` |

浏览器控制面未回传可复核的实际 viewport 值，因此本场不承担精确视口验收。
冻结自动化已另行在 `1440×900` 和 `1280×720` 两个项目通过。

## 周初判断

agent 在首次管理操作前复述：

1. 粮食预计轻度缺口为 `3–11`；
2. 水泵需要 2 个预防维修块，但只安排了 1 个；
3. 维修保障处于 `3–5` 的脆弱平衡。

## 候选编辑、撤销与地图

- 通过一次 `action-0001` 批量把林禾第 1 日 B3 与第 2 日 B3 共两格改为学习；
- 粮食预测由 `3–11` 变为 `0–8`，维修保障维持 `3–5`；
- 撤销后两格恢复为物流与农务，撤销按钮禁用；
- 地图从北侧绕行 `860 米 / 96 分钟 / 损耗 6` 切换为南侧短通路
  `470 米 / 52 分钟 / 损耗 2`；
- 短通路投入 1 点维修保障后，粮食预测变为 `7–15`，维修预测变为 `2–4`。

## 两周闭环

- 第一周周三 09:00，因预防维修不足发生水泵停机；
- 粮食预测由 `7–15` 变为 `7`，实际损失 8 单位粮食并占用 4 点维修；
- 第一周复盘显示计划 `7–15`、实际 `7`；
- 第二周继承南侧短通路；
- 林禾请求逾期后按公开默认保留农务，并记录“林禾记住了这次沉默”；
- 第二周复盘显示计划 `7`、实际 `7`；
- 最终两周进度 `100%`，时钟为第 14 日 23:00。

## 导出与清空

- 点击下载后页面明确回执：`已导出 tick 2010 的匿名记录。`
- 浏览器控制层没有捕获下载事件，agent 未越界读取下载文件；
- 该缺口由同一 RC 的 Chromium E2E 真实解析下载 payload 补证；
- 点击结束并清空后回到“开始匿名新会话”入口；
- 页面显示上一场已从网页内存清空。

## 裁定

`TECH-A98_UI_AUDIT=PASS`

本场证明 RC3 的候选编辑、撤销、地图因果、两周推进、导出回执与清空路径可由独立
agent 仅通过 UI 完成。它不计入正式七样本，不证明 Gate 1H，也不解锁 Gate 2。
