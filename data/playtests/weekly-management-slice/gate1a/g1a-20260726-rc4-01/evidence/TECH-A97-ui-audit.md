# Gate 1A RC4 独立技术 UI 审计

## 身份与边界

- 审计编号：`TECH-A97`
- UI 匿名编号：`A97`
- 计入 Gate 1A：否
- 执行方式：全新独立 agent，只使用玩家页面
- 禁止项：未读取或修改仓库、源码、测试、设计、Issue、开发者控制台或配置
- 结论：`PASS`，无产品流程阻断

本记录只验证冻结前的 RC4，不是 A01–A07 正式样本。

## 可见 RC 元数据

| 字段 | UI 可见值 |
|---|---|
| build ID | `g1-rc-20260726.4` |
| Git SHA | `a769877871f232b3722c02a184ccbc3426950887` |
| artifact hash | `0c84ce60f0fd3305a90fac23fcdfbd47cd67b16a6a0b2b72751da11de6cb6b58` |
| initial state hash | `fnv1a32-33a16fbf` |
| 场景版本 | `0.4.0` |
| 固定种子 | `104729` |
| Session | `a94542cd-e615-4e36-b2c4-652a7450a2b3` |

## 水泵状态一致性

- 将林禾第一周周二 B2 从“休息 · 基础计划”改为“维修 · 本周例外”；
- 摘要即时变为“水泵检修已安排 2 个维修块 / 乔磐与林禾各 1 块”；
- 定位卡同步显示“维修 · 本周例外 / 水泵检修第 2 / 2 块”；
- 粮食预测由 `3–11` 变为 `9`，维修保障由 `3–5` 变为 `4`。

第一处 RC3 冻结阻断已关闭。

## 地图与第一周

- 北侧绕行 `860 米 / 96 分钟 / 损耗 6` 切换为南侧短通路
  `470 米 / 52 分钟 / 损耗 2`；
- 粮食预测由 `9` 变为 `13`，维修保障由 `4` 变为 `3`；
- 周三事件“水泵异常，检修奏效”使粮食预测由 `13` 变为 `12–13`；
- 第一周复盘显示计划 `13`、实际 `12`。

## 第二周终局一致性

- 接受林禾学习请求后，主预测由 `14–15` 变为 `12–13`；
- 第二周终局主预测保持 `12–13`；
- 第二周复盘显示计划 `12–13`、实际 `12`；
- 主预测与复盘计划区间一致。

第二处 RC3 冻结阻断已关闭。

## 导出与清空

- 页面回执：`已导出 tick 2010 的匿名记录。`
- 浏览器控制面在 10 秒内没有暴露下载事件，agent 未越界读取下载文件；
- 同一 RC 的 Chromium E2E 已真实解析下载 payload；
- 结束并清空后回到固定初态入口；
- 页面显示上一场已从网页内存清空。

## 裁定

`TECH-A97_UI_AUDIT=PASS`

该技术审计不进入正式七样本，也不证明 Gate 1H 或解锁 Gate 2。
