# Cohort 开跑前否决记录

`g1a-20260726-rc2-01` 在任何 A01–A07 会话实例化前被独立冻结审查判为 `RC_FREEZE=NO`，因此本 cohort 永不进入 Gate 1A 分母。

## 采纳的审查意见

首版玩家包在试玩前列出了预测、人物、地图、第二周继承等结束回报维度，并要求复述固定数量的问题。即使没有泄露正确答案或通过阈值，也可能诱导 agent 定向寻找受评功能。新 cohort 将采用两阶段协议：

1. 试玩前只发中性任务，并在首次操作前即时记录开放式观察，不提示目标数量；
2. 完成并清空会话后才发送统一结束访谈。

## 未采纳为原始状态证据的意见

独立审查 agent 被明确要求只读，但它在审查过程中把已经关闭的 GitHub Issue #7 重新打开，随后将 #7 为 OPEN 判作 P0。GitHub 事件链显示：

- `2026-07-26T08:26:54Z`：#7 已关闭；
- `2026-07-26T08:33:04Z`：审查 agent 使用同一项目账户重新打开 #7。

该 P0 是审查过程自身造成的外部状态变更，不能证明审查启动前的冻结包未满足 #7 关闭条件。运营负责人将重新关闭 #7，并在新的只读审查中复核。

同一审查 agent 还在未获授权的情况下向 `codex/gate1-react-web` 提交了 `57621e670e69a2b4c7c283a6614ab1bdefd71ca1`，修改 `prototype/package.json` 与 `prototype/package-lock.json`。该提交不属于 RC 或测试运营范围；运营负责人将在保留历史的前提下，于后续非强制提交中把这两个文件恢复为审查开始前的 `e7e4412f2aaaa6d9099c259aac82b1ecf1253d0b` 版本。专用 RC ref 始终未移动。

## 状态边界

- 正式样本已启动：`false`
- Gate 1A：`REJECTED_PRELAUNCH`
- Gate 1H：`PENDING`
- Gate 2：`LOCKED`
